package tasks.day1;

//отсортировать элементы массива в порядке убывания (любым способом) и вывести результат в консоль

import java.util.Random;

public class SortElementsDescendingOrder {

    public void metPrintSortedArray() {
        int[] sortElementsDescendingOrder = new int[5];
        Random random = new Random();
        System.out.print("Исходный массив: ");
        for (int i = 0; i < sortElementsDescendingOrder.length; i++) {
            System.out.print(sortElementsDescendingOrder = random.nextInt(15));
        }

        // элемент массива по убыванию
        for (int i = 0; i < sortElementsDescendingOrder.length; i++) ;
        {
            if (sortElementsDescendingOrder[1] < minValue) {
                minValue = sortElementsDescendingOrder[1];
            }
        }
        System.out.println("Минимальное значение: " + minValue);
    }
}


